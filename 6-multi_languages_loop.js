const languages = ["C is fun", "Python is cool", "JavaScript is amazing"]; // Create an array with the strings

for (let i = 0; i < languages.length; i++) { // Loop through the array
    console.log(languages[i]); // Print each string in the array
}
